enum LoadingState {
  beforeLoad = "beforeLoad",
  loading = "loading",
  loaded = "loaded",
};
const enlighLoadingStte = {
    [LoadingState.beforeLoad]: "before load"
};

console.log(enlighLoadingStte);
